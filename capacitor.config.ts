import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farasi.beemore',
  appName: 'BeeMore',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
