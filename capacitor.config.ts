import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gt.asms.beemoree',
  appName: 'BeeMore',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
